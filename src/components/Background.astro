---
import Plum from '~/components/bgs/Plum.astro'
import Dots from '~/components/bgs/Dots.astro'

interface Props {
  type: string
}

const { type } = Astro.props
---

{type === 'plum' && <Plum />}
{type === 'dots' && <Dots />}
